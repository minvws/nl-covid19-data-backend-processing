-- Copyright (c) 2020 De Staat der Nederlanden, Ministerie van   Volksgezondheid, Welzijn en Sport.
-- Licensed under the EUROPEAN UNION PUBLIC LICENCE v. 1.2 - see https://github.com/minvws/nl-contact-tracing-app-coordinationfor more information.

IF NOT EXISTS(SELECT * FROM sys.sequences WHERE object_id = OBJECT_ID(N'[dbo].[SEQ_VWSSTATIC_CBS_AGEGROUP_MAPPING]') AND type = 'SO')
CREATE SEQUENCE SEQ_VWSSTATIC_CBS_AGEGROUP_MAPPING
    START WITH 1
    INCREMENT BY 1;
GO

CREATE TABLE VWSSTATIC.CBS_AGEGROUP_MAPPING(
    [ID] INT PRIMARY KEY NOT NULL DEFAULT (NEXT VALUE FOR [dbo].[SEQ_VWSSTATIC_CBS_AGEGROUP_MAPPING]),
    [KEY] VARCHAR(10),
	[TITLE] VARCHAR(25),
	[DESCRIPTION] VARCHAR(100),
	[CATEGORYGROUPID] VARCHAR(10),
	[DATE_LAST_INSERTED] DATETIME DEFAULT GETDATE()
);
